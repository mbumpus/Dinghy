# External QA pass (Codex), 2026-06-11. All fixed same day except BUG-003 (user rebuilds DMG).
- { id: BUG-001, severity: high,   area: security,     status: fixed, description: "Imported camera roles (and width/height/fps) rendered without HTML escaping; XSS reachable from malicious YAML with native bridge exposure", fix: "esc() on all renderCamList interpolations; type coercion in Importer._settings; strict CSP meta added", verification: "imported <img onerror> role renders inert as text" }
- { id: BUG-002, severity: high,   area: dependencies, status: fixed, description: "Electron 33.4.11 carried high-severity advisories", fix: "Upgraded electron 33→42.4.0 and electron-builder 25→latest; npm audit now 0 vulnerabilities", verification: "npm audit clean; launch smoke on 42 passes" }
- { id: BUG-003, severity: high,   area: release,      status: open-user, description: "Packaged DMG stale (v0.3.0 artifact, pre-ONVIF)", fix: "User will rebuild via npm run dist after this round", verification: "n/a" }
- { id: BUG-004, severity: high,   area: undo,         status: fixed, description: "Polygon delete/delete-all undo restored into whichever camera was active at undo time", fix: "Delete actions capture camId; restorePolygon(s) target the original camera", verification: "delete on cam A, switch to cam B, undo → polygon back on A only" }
- { id: BUG-005, severity: high,   area: persistence,  status: fixed, description: "Clear All Data left runtime images and the reset's debounced save recreated the localStorage key", fix: "Images map wiped (snapshot kept for undo); Persist.clear cancels pending debounce", verification: "key absent 800ms after clear; Images empty" }
- { id: BUG-006, severity: medium, area: validation,   status: fixed, description: "Empty zone name silently auto-named item_N instead of blocking", fix: "Zone name validated before fallback; empty blocks with inline error (masks may still auto-name)", verification: "empty-name zone blocked; named commits" }
- { id: BUG-007, severity: medium, area: import,       status: fixed, description: "Top-level motion.mask/objects.filters ignored when cameras: present", fix: "Importer also parses root zones/motion/objects as snippet pseudo-camera", verification: "global motion mask imports alongside cameras" }
- { id: BUG-008, severity: medium, area: import,       status: fixed, description: "Pixel coords converted using persisted image meta without a loaded image (possibly stale dims)", fix: "Conversion requires a live loaded image; otherwise skip with warning", verification: "pixel zone skipped with warning when only meta exists" }
- { id: BUG-009, severity: medium, area: editing,      status: fixed, description: "Mouseup before queued rAF dropped the final drag movement", fix: "Mouseup cancels rAF and commits pending position before endDrag; Escape path also cancels rAF", verification: "code path review; drag math regression green" }
- { id: BUG-010, severity: medium, area: onvif,        status: fixed, description: "Credentials persisted in OnvifUI.devices[].creds; errors sanitized password only", fix: "Creds never stored in JS — read from visible inputs at click time (profiles injected per-device, inputs preserved); main.js scrubs username and password from errors", verification: "no creds field in devices objects; scrub covers both parts" }
- { id: BUG-011, severity: medium, area: persistence,  status: fixed, description: "Corrupt/unknown stored workspace silently discarded, key left in place, no warning", fix: "Persist.load removes bad value and returns discarded flag; init shows toast", verification: "corrupt JSON → key removed + discarded:true" }
- { id: BUG-012, severity: medium, area: multicamera,  status: fixed, description: "Deleting the active camera carried its draft onto the default tab", fix: "deleteCamera swaps in default's parked draft and drops the deleted camera's draft", verification: "draft empty on default after deleting active camera mid-draw" }
- { id: BUG-013, severity: low,    area: editing,      status: fixed, description: "No vertex hover highlight (spec 05 required affordance)", fix: "Hovered vertex renders enlarged accent-filled handle; cleared on mouseleave", verification: "visual + hover-key rerender logic" }
- { id: BUG-014, severity: low,    area: architecture, status: fixed, description: "YAML scope was global UI state calling View.render directly", fix: "yamlScope moved into Store state with setYamlScope action; persisted; select synced in render", verification: "scope in Store.state; changeYamlScope is a Store action" }
- { id: BUG-015, severity: low,    area: specs,        status: fixed, description: "Spec 08 invalid JSON (semicolon outside string)", fix: "Removed stray semicolon", verification: "all 10 spec files json-parse clean" }
- { id: BUG-016, severity: low,    area: docs,         status: fixed, description: "README said four bridge functions; six exist", fix: "Updated; also documents CSP", verification: "README review" }
